return {
  "nvim-tree/nvim-tree.lua",
  opts = {
    filters = {
      git_ignored = false, -- Show .gitignored files by default
      dotfiles = false, -- Show dot/hidden files by default
    },
  },
}
